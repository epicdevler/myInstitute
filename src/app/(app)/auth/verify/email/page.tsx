import { Heading } from "@chakra-ui/react";

/**
 * Page handling actual email verification after the request has been made.
 * This page is typically accessed via a link sent to the user's email or it could be for entering code sent via email.
 * It confirms the user's email address and activates their account.
 * @returns React.ReactNode
 */
export default function Page(){
    return <Heading>Email Verification</Heading>
}